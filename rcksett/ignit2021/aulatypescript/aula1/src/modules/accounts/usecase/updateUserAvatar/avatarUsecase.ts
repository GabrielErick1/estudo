import { InterfaceAccount } from "../../repositories/IusersInterface";
import { inject, injectable } from "tsyringe";
class avatarUsecase {
    constructor(private avatarRepository: InterfaceAccount){}
   execute(){

  }
}